# Sample corpus

Put plain-text (`.txt`) reference books in this directory. To use one or more
different corpus locations, edit `corpus_dirs` in the repository's
`config.json`. Paths may be relative to the repository root or absolute.
