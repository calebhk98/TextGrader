{
  "manuscript": "MANUSCRIPT.md",
  "chapters_dir": "chapters",
  "characters_dir": "characters",
  "corpus_profile": "data/prose_reference.json",
  "corpus_dirs": [],
  "analysis": {
    "comparison_unit": "unknown",
    "min_sentences_for_corpus": 40,
    "min_words_for_corpus": 500
  },
  "text_processing": {
    "strip_gutenberg": true,
    "strip_markdown_headings": true,
    "strip_transcript": true,
    "drop_marker_paragraphs": true,
    "normalize_quotes": false,
    "segmenter": "auto",
    "language": "en",
    "segmenter_clean": false,
    "transcript": null
  },
  "nlp": {
    "model": "en_core_web_sm",
    "disable": [
      "ner"
    ],
    "max_chars_per_chunk": 400000,
    "max_words": null
  },
  "regex": {
    "engine": "auto",
    "timeout_seconds": 2.0
  },
  "corpus_builder": {
    "output_dir": "corpus",
    "count": 40,
    "minimum_count": 30,
    "providers": [
      "gutenberg"
    ],
    "metadata_providers": [
      "openlibrary",
      "google_books"
    ],
    "work_types": [
      "fiction"
    ],
    "genres": [],
    "include_terms": [],
    "exclude_terms": [],
    "include_authors": [],
    "max_authors": null,
    "max_books_per_author": 2,
    "metric_selection": "enabled"
  },
  "metrics": {
    "repeated_ngrams": {
      "enabled": true,
      "sizes": [
        3,
        4,
        5,
        6
      ],
      "min_count": 2,
      "max_reported": 40
    },
    "sentence_openings": {
      "enabled": true,
      "words": 3
    },
    "local_repetition": {
      "enabled": true,
      "windows": [
        50,
        100,
        250
      ]
    },
    "function_words": {
      "enabled": false
    },
    "mattr": {
      "enabled": true,
      "window": 100
    },
    "punctuation": {
      "enabled": true
    },
    "length_quantiles": {
      "enabled": true
    },
    "pov_pronouns": {
      "enabled": true,
      "block_words": 500
    },
    "dialogue_contractions": {
      "enabled": true
    },
    "dialogue_tags": {
      "enabled": true
    },
    "sentence_segmentation": {
      "enabled": false
    },
    "passive_voice": {
      "enabled": false
    },
    "clause_structure": {
      "enabled": false
    },
    "pos_distribution": {
      "enabled": false
    },
    "tense_consistency": {
      "enabled": false
    },
    "nominalizations": {
      "enabled": false
    },
    "character_voice": {
      "enabled": false
    },
    "distribution_shape": {
      "enabled": true,
      "bands": 5
    },
    "sentence_length_autocorrelation": {
      "enabled": false,
      "lags": [
        1,
        2,
        3
      ]
    },
    "sentence_length_deltas": {
      "enabled": false
    },
    "sentence_run_lengths": {
      "enabled": false,
      "short_max": 8,
      "long_min": 25
    },
    "sentence_length_entropy": {
      "enabled": false
    },
    "paragraph_rhythm": {
      "enabled": false
    },
    "single_sentence_paragraph_runs": {
      "enabled": false
    },
    "dependency_distance": {
      "enabled": false
    },
    "parse_depth": {
      "enabled": false
    },
    "finite_clauses": {
      "enabled": false
    },
    "clause_types": {
      "enabled": false
    },
    "coordination_ratio": {
      "enabled": false
    },
    "opening_patterns": {
      "enabled": false,
      "depth": 2,
      "max_reported": 25
    },
    "mtld": {
      "enabled": false,
      "threshold": 0.72
    },
    "hdd": {
      "enabled": false,
      "sample": 42
    },
    "word_rarity": {
      "enabled": false,
      "language": "en"
    },
    "repetition_distance": {
      "enabled": false,
      "min_length": 4
    },
    "lemma_repetition": {
      "enabled": false
    },
    "adjacent_sentence_similarity": {
      "enabled": false,
      "model": "all-MiniLM-L6-v2"
    },
    "local_similarity_window": {
      "enabled": false,
      "model": "all-MiniLM-L6-v2",
      "windows": [
        3,
        5
      ]
    },
    "paragraph_similarity": {
      "enabled": false,
      "model": "all-MiniLM-L6-v2"
    },
    "duplicate_sentence_clusters": {
      "enabled": false,
      "model": "all-MiniLM-L6-v2",
      "threshold": 0.85,
      "max_reported": 30
    },
    "dialogue_channels": {
      "enabled": false
    },
    "dialogue_turn_lengths": {
      "enabled": false
    },
    "speaker_style": {
      "enabled": false,
      "min_turns": 8
    },
    "dialogue_attribution": {
      "enabled": false
    },
    "dialogue_runs": {
      "enabled": false
    },
    "speaker_function_words": {
      "enabled": false,
      "min_turns": 8
    },
    "sentence_initial_connectives": {
      "enabled": false
    },
    "causal_connectives": {
      "enabled": false
    },
    "hedges_boosters": {
      "enabled": false
    },
    "rhetorical_constructions": {
      "enabled": false,
      "max_reported": 30,
      "min_count": 3
    },
    "narration_pov": {
      "enabled": false
    },
    "pov_block_confidence": {
      "enabled": false,
      "block_words": 1000
    },
    "entity_pronoun_ratio": {
      "enabled": false,
      "enable_ner": true
    },
    "punctuation_profile": {
      "enabled": false
    },
    "punctuation_entropy": {
      "enabled": false
    },
    "punctuation_patterns": {
      "enabled": false,
      "max_reported": 25
    },
    "chapter_zscores": {
      "enabled": false,
      "window_words": 2500
    },
    "rolling_drift": {
      "enabled": false,
      "window_words": 2500
    },
    "change_points": {
      "enabled": false,
      "window_words": 2500,
      "penalty": 2.0
    },
    "coherence_suite": {
      "enabled": false,
      "features": {
        "lexical": true,
        "lexical_wordnet": false,
        "lexical_wordnet_hypernym": false,
        "semantic": true,
        "entity": true,
        "coreference": false,
        "rst": false,
        "connectives": true,
        "order_permutation": true
      },
      "_requires_coreference": "features.coreference needs the 'fastcoref' package (which pulls in torch and a transformer encoder) plus a one-time download of its default model (biu-nlp/f-coref, ~90M parameters); without it, every discourse.coherence_*_coref metric id reports 'unavailable' with that reason. The flag defaults to false and this note is stripped before it ever reaches the metric, so nothing downloads unasked.",
      "_requires_lexical_wordnet": "features.lexical_wordnet and features.lexical_wordnet_hypernym both need the nltk 'wordnet' corpus downloaded (python -m nltk.downloader wordnet) -- the nltk package alone is not enough. Without it, discourse.coherence_lexical_chain_coverage_wordnet and discourse.coherence_lexical_chain_coverage_hypernym report 'unavailable' with that instruction; every other channel is unaffected.",
      "_requires_semantic": "features.semantic needs 'sentence-transformers' installed to embed with the model named by semantic_model (default all-MiniLM-L6-v2, downloaded on first use). Without it, the semantic findings degrade to a TF-IDF lexical-overlap proxy rather than disappearing, and each finding's warning/distribution.backend says so.",
      "_requires_entity_graph": "the entity co-occurrence graph metric ids (discourse.coherence_entity_graph_density and its _coref variant) need 'networkx'; without it they report 'unavailable' while the rest of the entity/coreference groups, which only need spaCy, are unaffected.",
      "_requires_transition_corpus_delta": "discourse.coherence_entity_grid_transition_corpus_delta needs a corpus profile built with coherence_suite enabled AND BOTH --parse-metrics and --model-metrics (this suite's cost is 'parse' and it requires sentence_transformers, so textgrader.corpus._metric_names needs both flags before it will call this suite's profile_vector); without one it reports 'no corpus entity-grid transition profile available'.",
      "_requires_rst": "features.rst needs the 'isanlp_rst' package (pip install isanlp-rst) plus its non-PyPI 'isanlp' dependency (pip install git+https://github.com/iinemo/isanlp.git); without either, every discourse.coherence_rst_* metric id reports 'unavailable' with that reason. Constructing the parser downloads about 5 GB of model checkpoints on first use and holds about 6 GB of RAM resident for as long as it stays loaded (cached once per process; never reloaded per document). Measured cost: loading the parser took about 69 seconds on a quiet CPU-only machine (this is a one-time, per-process cost and is never counted against rst_max_seconds); parsing itself measured 1.9-6.7 seconds per sentence across several runs on a shared container, roughly 2 seconds per sentence at the low end. That is why this feature never parses a whole document: it samples rst_passages passages of rst_passage_sentences sentences each, spread across the book with a fixed rst_seed, capped at rst_max_sentences total sentences and rst_max_seconds of wall time (default 420 seconds, sized with margin so the default 8-passage/6-sentence-each sample -- about 48 sentences, roughly 90-320 seconds at the measured per-sentence range -- can complete on an uncontended machine rather than routinely hitting the cap). If rst_max_seconds is reached, every discourse.coherence_rst_* finding's sample_size (passages actually parsed) drops below its min_sample (passages the deterministic sample called for), which grade.py reports as insufficient_data and withholds from corpus comparison automatically -- so a run truncated by a slow or busy machine is never silently compared against a reference profile built from a complete sample, or against each other. The flag defaults to false and this note is stripped before it ever reaches the metric, so nothing downloads or loads unasked.",
      "min_word_len": 3,
      "chain_gap": 3,
      "chain_min_length": 2,
      "chain_hypernym_max_distance": 3,
      "semantic_model": "all-MiniLM-L6-v2",
      "semantic_low_tail_threshold": 0.15,
      "entity_lookback_sentences": 10,
      "entity_max_tracked": 150,
      "entity_graph_window_sentences": 3,
      "entity_min_mentions_for_graph": 2,
      "coreference_model": "biu-nlp/f-coref",
      "coreference_max_words": 4000,
      "rst_model": "tchewik/isanlp_rst_v3",
      "rst_model_version": "rstdt",
      "rst_passages": 8,
      "rst_passage_sentences": 6,
      "rst_max_sentences": 60,
      "rst_max_seconds": 420.0,
      "rst_seed": 0,
      "connective_max_reported": 25,
      "permutations": 50,
      "seed": 0,
      "order_min_sentences_per_paragraph": 4,
      "order_max_sentences_per_paragraph": 40,
      "order_max_paragraphs_sampled": 30,
      "order_max_paragraphs_for_doc": 60
    },
    "randomness_suite": {
      "enabled": false,
      "features": {
        "char_entropy": true,
        "compression": true,
        "complexity_measures": true,
        "language_model": true,
        "punctuation_sequence": true,
        "pos_dependency": false,
        "corruption_baselines": true,
        "lexical_gibberish": true,
        "ppm_language_model": true,
        "letter_bigram_divergence": true,
        "kenlm_language_model": false,
        "neural_language_model": false,
        "textdescriptives_cross_check": false,
        "gibberish_detector_package": false,
        "ncd_against_corpus": false
      },
      "_complexity_measures_requires": "needs numpy for the permutation/spectral/SVD/ApEn/SampEn family; without it every one of those five findings reports 'unavailable' (the Lempel-Ziv complexity finding in the same feature is dependency-free and still runs).",
      "_lexical_gibberish_requires": "unknown_word_rate needs wordfreq (pip install wordfreq); without it that one finding reports 'unavailable' -- letter_frequency_divergence, consonant_cluster_rate, immediate_repeat_rate and char_run_length_entropy in this same feature are dependency-free and still run.",
      "_compression_requires": "zlib/gzip/bz2/lzma are stdlib and always work. zstd/brotli/lz4/ppmd each need their own pip package (zstandard/brotli/lz4/pyppmd); snappy needs python-snappy AND the system libsnappy-dev package (apt/brew, not pip). Any one missing degrades only its own compression_ratio_<name> finding to 'unavailable'.",
      "_pos_dependency_requires": "needs the shared spaCy parse (spacy + a downloaded model, e.g. `python -m spacy download en_core_web_sm`); without it every finding in this feature reports 'unavailable'. Off by default because it forces a parse cost onto every user regardless of whether they wanted this one channel.",
      "_ppm_language_model_requires": "needs pyppmd (pip install pyppmd); without it ppm_cross_entropy reports 'unavailable'.",
      "_letter_bigram_divergence_requires": "needs the shipped data/prose_reference.json corpus profile (its word_frequency table); without it, or if it is unreadable, letter_bigram_divergence reports 'unavailable' naming the missing/broken file.",
      "_kenlm_language_model_requires": "needs the `kenlm` Python wheel (query-time bindings) AND a compiled `lmplz` trainer binary that pip cannot provide -- build KenLM from source (a C++ compiler and Boost; see https://github.com/kpu/kenlm#compiling), then put the resulting `lmplz` on PATH or set kenlm_lmplz_path to its full path. Without either, kenlm_cross_entropy reports 'unavailable' naming exactly which piece (the wheel or the trainer) is missing and how to get it; nothing else in this suite is affected.",
      "_neural_language_model_requires": "needs torch + transformers (multi-gigabyte install; the configured neural_lm_model, distilgpt2 by default, is downloaded or read from a local cache on first use). Without either package, neural_lm_perplexity reports 'unavailable'. Caveat even when available: subword (BPE) tokenization can score purely random letters as MORE 'fluent' (lower perplexity) than grammatically word-order-scrambled real prose, so this channel is NOT a gibberish detector -- see the module docstring and this feature's own finding.distribution.warning_not_a_gibberish_detector.",
      "_textdescriptives_cross_check_requires": "needs its own spaCy pipeline plus the textdescriptives package (pip install textdescriptives); without either, textdescriptives_word_perplexity reports 'unavailable'.",
      "_gibberish_detector_package_requires": "needs the gibberish-detector package (pip install gibberish-detector). Off by default so that corpus profiling never trains a fresh character-bigram model as a side effect of enabling this suite. Trains on this document's own held-out split (never on the package's bundled reference file, never on a corpus profile); without the package, gibberish_detector_score reports 'unavailable'. Reads its own gibberish_detector_charset tunable, and is an independent channel beside consonant_cluster_rate, not a replacement -- the two are expected to disagree, see the module docstring.",
      "_ncd_against_corpus_requires": "ncd_corpus_dirs pointed at one or more directories of readable reference .txt/.md files, present on disk at GRADING time (not just when a corpus profile was built -- this measurement reads the folder directly, it is never served from a cached profile). Off by default for the same reason as gibberish_detector_package: corpus profiling must never open a corpus folder as a side effect of enabling this suite. Without a reachable directory this degrades to unavailable() naming what was missing. Mirrors stylometry_suite's identically-named ncd_corpus_dirs option but keeps its own ncd_corpus_algorithm/ncd_corpus_max_reference_documents/ncd_corpus_max_bytes bounds, independent of that suite's. ncd_corpus_algorithm defaults to lzma, not zlib: zlib/gzip's DEFLATE window is a fixed 32768 bytes, smaller than 2 * ncd_corpus_max_bytes (100000 by default), so it cannot see back across the concatenation NCD needs and would score an identical document the same as an unrelated one (measured: 0.97 vs 0.98 -- no discrimination). lz4 and snappy have the same problem past roughly 65536 bytes combined. Choosing one of those four algorithms with byte caps that exceed its window degrades to unavailable() naming the exact window and a safe max_bytes instead of silently reporting a meaningless number; lzma/zstd/brotli/bz2 are unaffected at this suite's byte caps.",
      "language": "en",
      "char_ngram_orders": [
        2,
        3,
        4,
        5,
        6
      ],
      "byte_ngram_order": 3,
      "word_ngram_orders": [
        1,
        2,
        3,
        4
      ],
      "punct_ngram_order": 3,
      "pos_ngram_order": 3,
      "dependency_ngram_order": 2,
      "lm_train_fraction": 0.7,
      "lm_smoothing_alpha": 0.5,
      "lm_max_chars": 150000,
      "lm_max_tokens": 60000,
      "entropy_token_cap": 100000,
      "renyi_orders": [
        0.5,
        2.0
      ],
      "tsallis_orders": [
        0.5,
        2.0
      ],
      "excess_entropy_max_order": 4,
      "mi_lags": [
        1,
        2,
        3
      ],
      "mi_max_tokens": 20000,
      "complexity_quadratic_cap": 1500,
      "permutation_order": 3,
      "lz_max_chars": 20000,
      "compression_algorithms": [
        "zlib",
        "gzip",
        "bz2",
        "lzma",
        "zstd",
        "brotli",
        "lz4",
        "snappy",
        "ppmd"
      ],
      "compression_level": 6,
      "compression_block_chars": 20000,
      "ncd_algorithm": "zlib",
      "corruption_seed": 1337,
      "corruption_permutations": 3,
      "corruption_sentence_fraction": 0.3,
      "corruption_char_order": 4,
      "corruption_word_order": 2,
      "corruption_max_chars": 20000,
      "consonant_cluster_min": 4,
      "ppm_max_order": 6,
      "kenlm_lmplz_path": "",
      "kenlm_order": 3,
      "kenlm_memory": "50M",
      "kenlm_max_train_chars": 200000,
      "kenlm_timeout_seconds": 30,
      "neural_lm_model": "distilgpt2",
      "neural_lm_max_chars": 6000,
      "textdescriptives_max_chars": 50000,
      "gibberish_detector_charset": "abcdefghijklmnopqrstuvwxyz",
      "ncd_corpus_dirs": [],
      "ncd_corpus_max_reference_documents": 10,
      "ncd_corpus_max_bytes": 100000,
      "ncd_corpus_algorithm": "lzma"
    },
    "absolutes": false,
    "banned_phrases": false,
    "check_edits": false,
    "dialogue_study": false,
    "number_report": false,
    "prose_check": false,
    "quotable": false,
    "quote_length": false,
    "register": false,
    "style_report": false,
    "tics": false,
    "verify_citations": false,
    "voice_separation": false,
    "logic_suite": {
      "enabled": false,
      "_requires": {
        "nli_entailment": "needs `transformers` (pip install transformers torch) plus a downloaded model checkpoint (nli_model, cross-encoder/nli-deberta-v3-small by default, fetched from Hugging Face on first use). This is the most expensive channel in this suite: loading the pipeline costs about 2.5s and scoring costs roughly 30ms/pair on an otherwise-idle CPU core, before nli_max_pairs even grows large. Without transformers or a reachable checkpoint, discourse.logic_nli_label_distribution and discourse.logic_nli_heuristic_agreement report 'unavailable' with the reason; no other feature in this suite is affected, and no model is ever loaded unless this key is explicitly true.",
        "coreference_resolution": "needs `fastcoref` (pip install fastcoref) plus its packaged model (biu-nlp/f-coref, fetched from Hugging Face on first use), loaded through textgrader.optional.shim_fastcoref_transformers to stay compatible with transformers 5.x. Without fastcoref, or if it fails to load, every pronoun-subject proposition is simply excluded from cross-sentence matching -- exactly the same result as leaving this feature off, just with a reason recorded instead of silence. This is a modifier, not a metric group of its own: it changes what feeds `propositions`, `nli_entailment`, `lexical_opposition` and `temporal_ordering` rather than adding metric ids.",
        "lexical_opposition": "needs `nltk` (pip install nltk) AND its WordNet corpus data downloaded separately -- the package alone is not enough. Run python -c \"import nltk; nltk.download('wordnet')\" once. Without the corpus data, discourse.logic_wordnet_antonym_candidates and discourse.logic_wordnet_hypernym_downgrade_rate report 'unavailable' with that exact command in the reason.",
        "temporal_ordering": "needs `python-dateutil` (pip install python-dateutil). Without it, discourse.logic_temporal_order_candidates reports 'unavailable'; the 'temporal' subtype inside discourse.logic_entity_attribute_conflict_candidates (features.propositions) is unaffected either way, since it only notices two differing years/dates, never orders them.",
        "semantic_role_labeling": "needs `transformers` (already required by nli_entailment; no new package) plus a downloaded checkpoint (srl_model, cu-kairos/propbank_srl_seq2seq_t5_small by default, fetched from Hugging Face on first use). One model-generation call per clause-level predicate examined (capped by srl_max_predicates, much smaller than proposition_cap because each call is far more expensive than the shared spaCy parse). Without transformers or a reachable checkpoint, discourse.logic_srl_role_pattern_consistency and discourse.logic_srl_argument_omission_rate report 'unavailable' with the reason.",
        "relation_extraction": "needs `transformers` (no new package) plus a downloaded checkpoint (relation_extraction_model, Babelscape/rebel-large by default, ~1.6GB, fetched from Hugging Face on first use). CLOSED-schema relation extraction (~200 Wikidata-style relation types), not open-domain OpenIE -- AllenNLP's OpenIE predictor cannot be installed here (see the module docstring's quoted pip dry-run failure) and Stanford OpenIE is Java. Hand-checked, not assumed: a plain fictional sentence stating nothing about where a character lives still generated a plausible-sounding 'residence' triple the text never asserts, so this channel is not simply quiet on fiction -- it can invent a schema-compatible relation instead of finding none. Without transformers or a reachable checkpoint, both discourse.logic_relation_extraction_* metrics report 'unavailable'.",
        "argument_mining": "needs `transformers` (no new package) plus a downloaded checkpoint (argument_mining_model, raruidol/ArgumentMining-EN-ARI-AIF-RoBERTa_L by default, ~1.4GB, fetched from Hugging Face on first use). A real Argument Relation Identification classifier (No-Relation/Inference/Conflict/Rephrase) applied to this suite's own therefore/because/however-adjacent clause pairs; connective_chain_length (features.connective_relations) stays a separate, unchanged, honestly-named proxy. Without transformers or a reachable checkpoint, both discourse.logic_argument_relation_* metrics report 'unavailable'.",
        "propbank_argument_structure": "needs `transformers` (already required by semantic_role_labeling; shares the same cached SRL frames, so turning both on with the same srl_model/srl_max_predicates costs one model call, not two) AND `nltk` (pip install nltk) with its propbank corpus data downloaded separately: run `python -m nltk.downloader propbank` once. Compares each SRL frame's filled ARG-N slots against the specific PropBank roleset (e.g. give.01's giver/thing-given/recipient) that best matches what was already filled -- a heuristic sense match, not gold word-sense disambiguation. Without transformers, a reachable SRL checkpoint, or the propbank corpus data, discourse.logic_propbank_argument_omission_rate reports 'unavailable' with the exact missing piece named in the reason.",
        "verbnet_class_consistency": "needs `nltk` (pip install nltk) with its verbnet corpus data downloaded separately: run `python -m nltk.downloader verbnet` once. No transformer model. Flags two different verbs used with the same two participants (shared non-pronoun subject and object) that share no VerbNet class at all. Without the verbnet corpus data, discourse.logic_verbnet_class_conflict_candidates reports 'unavailable' with that exact download command in the reason.",
        "framenet_frame_consistency": "needs `nltk` (pip install nltk) with its framenet_v17 corpus data downloaded separately: run `python -m nltk.downloader framenet_v17` once. No transformer model, but the first lookup in a process costs several seconds (nltk builds an internal FrameNet lemma index on first use; every later lookup is a fast dict access). The same 'same participants, different verb' check as verbnet_class_consistency, against FrameNet's frame inventory instead of VerbNet's classes -- kept as an independent channel because the two resources agreeing or disagreeing is itself data. Without the framenet_v17 corpus data, discourse.logic_framenet_frame_conflict_candidates reports 'unavailable' with that exact download command in the reason."
      },
      "features": {
        "negation_and_quantifiers": true,
        "connective_relations": true,
        "propositions": true,
        "modal_argument_position": true,
        "nli_entailment": false,
        "coreference_resolution": false,
        "lexical_opposition": false,
        "temporal_ordering": false,
        "semantic_role_labeling": false,
        "relation_extraction": false,
        "argument_mining": false,
        "propbank_argument_structure": false,
        "verbnet_class_consistency": false,
        "framenet_frame_consistency": false
      },
      "window_sentences": 6,
      "max_pairs": 200,
      "max_comparisons": 50000,
      "max_evidence": 20,
      "proposition_cap": 20000,
      "connective_min_words": 4,
      "repeated_assertion_min_words": 5,
      "coreference_max_chars": 20000,
      "nli_model": "cross-encoder/nli-deberta-v3-small",
      "nli_max_pairs": 60,
      "nli_batch_size": 16,
      "srl_model": "cu-kairos/propbank_srl_seq2seq_t5_small",
      "srl_max_predicates": 40,
      "relation_extraction_model": "Babelscape/rebel-large",
      "_relation_extraction_model_license": "Babelscape/rebel-large is licensed CC BY-NC-SA 4.0: non-commercial use only. TextGrader's own code is MIT, but whoever downloads and runs this model is bound by the model's licence. Swap in a commercially licensed model here before enabling relation_extraction for commercial work.",
      "relation_extraction_max_sentences": 40,
      "argument_mining_model": "raruidol/ArgumentMining-EN-ARI-AIF-RoBERTa_L",
      "_argument_mining_model_license": "raruidol/ArgumentMining-EN-ARI-AIF-RoBERTa_L is licensed CC BY-NC-SA 4.0: non-commercial use only. TextGrader's own code is MIT, but whoever downloads and runs this model is bound by the model's licence. Swap in a commercially licensed model here before enabling argument_mining for commercial work.",
      "argument_mining_max_pairs": 40
    },
    "timeseries_suite": {
      "enabled": false,
      "sequences": [
        "sentence_words",
        "paragraph_words",
        "sentence_punctuation"
      ],
      "feature_groups": [
        "dispersion",
        "acf",
        "trend",
        "turning_points",
        "runs"
      ],
      "lags": [
        1,
        2,
        3
      ],
      "pacf_max_lag": 5,
      "window_words": 2000,
      "rolling_window": 10,
      "change_point_penalty": 2.0,
      "page_hinkley_delta": 0.005,
      "page_hinkley_lambda": 3.0,
      "permutation_entropy_order": 3,
      "permutation_entropy_delay": 1,
      "dfa_min_box": 4,
      "piecewise_segments": 2,
      "detrend": false,
      "embedding_model": "all-MiniLM-L6-v2",
      "language": "en",
      "wavelet_name": "db4",
      "wavelet_max_level": 5,
      "tsfresh_feature_set": "minimal",
      "tsfresh_max_features": 25,
      "adwin_delta": 0.002,
      "topic_n_topics": 4,
      "topic_model": "nmf",
      "topic_random_state": 42,
      "topic_max_features": 2000,
      "min_lengths": {},
      "max_findings": 200,
      "_requires_catch22_and_catch24": "pycatch22 (pip install pycatch22). Without it, every catch22_* feature group and both catch24_raw_mean/catch24_raw_variance report 'unavailable' with an install hint instead of a number.",
      "_requires_wavelets": "PyWavelets (pip install PyWavelets). Without it, wavelet_energy and wavelet_entropy report 'unavailable' with an install hint instead of a number.",
      "_requires_stationarity": "statsmodels (pip install statsmodels) for a real Augmented Dickey-Fuller test. Without it, the 'stationarity' feature group does NOT go unavailable: it falls back to a labelled, weaker half-split standardized mean-gap proxy and says so in its warning.",
      "_requires_change_points": "ruptures (pip install ruptures) for real PELT change-point search. Without it, the 'change_points' feature group does NOT go unavailable: it falls back to a labelled single-split maximum-mean-shift scan (at most one change point can ever be found) and says so in its warning.",
      "_requires_embedding_sequences": "sentence-transformers (pip install sentence-transformers, in requirements-embeddings.txt) plus a downloaded sentence-embedding model (embedding_model above, default all-MiniLM-L6-v2, fetched on first use). Needed only if 'sequences' explicitly names sentence_similarity_prev or sentence_distance_centroid -- neither is ever selected by default. Without it, both sequences fall back to a TF-IDF lexical-overlap proxy, a different quantity, labelled as such (backend=lexical) in every finding's warning.",
      "_requires_tsfresh": "tsfresh (pip install tsfresh). Needed only if 'feature_groups' explicitly names 'tsfresh' -- it is never selected by default. Without it, the 'tsfresh' feature group reports 'unavailable' with an install hint instead of a number.",
      "_requires_adwin": "river (pip install river). Needed only if 'feature_groups' explicitly names 'adwin' -- it is never selected by default. Without it, the 'adwin' feature group reports 'unavailable' with an install hint instead of a number. Runs beside, not instead of, the dependency-free 'page_hinkley' detector.",
      "_requires_sentiment_and_emotion_sequences": "vaderSentiment (pip install vaderSentiment) for sentence_sentiment_compound, and NRCLex (pip install NRCLex) for sentence_emotion_valence. Needed only if 'sequences' explicitly names one of them -- neither is ever selected by default. Both packages bundle their own lexicon and need no separate download. Without the relevant package, that sequence's findings report 'unavailable' with an install hint instead of a number.",
      "_requires_topic_sequence": "scikit-learn (pip install scikit-learn) for window_topic_id's one-time NMF/LDA topic-model fit (settings: topic_n_topics, topic_model, topic_random_state, topic_max_features above). Needed only if 'sequences' explicitly names window_topic_id -- it is never selected by default, and its fit never runs during corpus profiling. Without scikit-learn, or with fewer windows than 2x topic_n_topics, that sequence's findings report 'unavailable' naming why."
    },
    "stylometry_suite": {
      "enabled": false,
      "_embedding_style_requires": "sentence-transformers plus a one-time model download (~90MB for the default all-MiniLM-L6-v2); without it, embedding_style/embedding_reference findings and the impostors_representation='embedding' branch degrade to unavailable() with that reason. Never loads on its own -- see the module docstring's 'The critical gating rule'.",
      "_embedding_reference_requires": "a corpus profile built with THIS SAME suite's features.embedding_style also enabled at profiling time (rebuild it with textgrader.corpus so feature_profiles['stylometry_suite'] holds per-book embedding vectors), plus sentence-transformers available at grading time to embed the manuscript the same way. Enabling embedding_reference alone, without rebuilding the profile, degrades to unavailable().",
      "_ncd_against_corpus_requires": "ncd_corpus_dirs pointed at one or more directories of readable reference .txt/.md files, present on disk at GRADING time (not just when the corpus profile was built -- this measurement reads the folder directly, it is never served from the cached profile). Without a reachable directory this degrades to unavailable() naming what was missing.",
      "_impostors_requires": "a corpus profile with author metadata (metadata.author on at least impostors_min_authors+1 books). impostors_representation='embedding' additionally needs the same profile rebuilt with features.embedding_style enabled (see _embedding_reference_requires).",
      "_impostors_classifier_requires": "the same author-labelled corpus profile as impostors, PLUS scikit-learn (already a project dependency; this is the one feature in this suite that actually fits it, rather than just importing numpy/scipy-adjacent helpers). Fits a real classifier per iteration, refitted from scratch each time -- never during corpus profiling, only at grading time with a complete profile in hand. Degrades to unavailable() naming whichever of these is missing.",
      "_pos_dependency_requires": "spaCy (en_core_web_sm) for the shared parse this feature forces -- tens of seconds on a novel-length manuscript; off by default for cost, not because it needs an unusual package.",
      "_lexicalrichness_crosscheck_requires": "the lexicalrichness package; without it this degrades to unavailable() and every other group in the suite is unaffected.",
      "_delta_family_requires": "nothing beyond corpus_reference's own corpus profile (feature_profiles['function_words']); hand-implemented arithmetic, not a package -- see the module docstring's section P for why the actual 'PyDelta' package could not be added as a dependency (quoted install/build failures) and why pystylometry_reference (below) is the package-backed half of the same family instead.",
      "_compression_algorithm_note": "compression_algorithm defaults to 'lzma', not 'zlib': zlib's dictionary is a fixed 32 KiB no matter the level, which is too small to compute a MEANINGFUL Normalized Compression Distance once either side of a comparison is much bigger than half that (ordinary for this suite's book-length inputs) -- ncd_open_close and ncd_against_corpus both refuse to report a number rather than silently give a meaningless one when this happens (see stylometry_suite.py's _ncd_window_guard for the measured evidence). Set compression_algorithm='zlib' only for short excerpts well under this limit.",
      "_pystylometry_reference_requires": "the pystylometry package (pip install pystylometry) PLUS ncd_corpus_dirs pointed at real reference .txt/.md files reachable on disk at GRADING time (reused from ncd_against_corpus's option, since both features read real reference text the same way). Without either, degrades to unavailable() naming which is missing.",
      "_corpus_reference_dependent_features": "corpus_reference, word_frequency_distance, author_language_model, corpus_language_model, impostors, impostors_classifier and delta_family all read the corpus_profile configured at the project level; without one configured (or reachable), each degrades to unavailable() rather than failing the run.",
      "features": {
        "character_ngrams": true,
        "byte_ngrams": true,
        "word_ngrams": true,
        "function_word_ngrams": true,
        "punctuation_shape": true,
        "word_shape": true,
        "affixes": true,
        "sentence_openings": true,
        "contractions_capitalization": true,
        "lexical_richness": true,
        "vocabulary_growth": true,
        "section_stability": true,
        "compression": true,
        "corpus_language_model": true,
        "corpus_reference": true,
        "pos_dependency": false,
        "author_language_model": true,
        "word_frequency_distance": true,
        "lexicalrichness_crosscheck": true,
        "embedding_style": false,
        "impostors": false,
        "impostors_classifier": false,
        "embedding_reference": false,
        "ncd_against_corpus": false,
        "delta_family": true,
        "pystylometry_reference": false
      },
      "char_ngram_orders": [
        2,
        3,
        4,
        5,
        6
      ],
      "byte_ngram_orders": [
        2
      ],
      "word_ngram_orders": [
        1,
        2,
        3
      ],
      "pos_ngram_orders": [
        1,
        2,
        3,
        4
      ],
      "dependency_ngram_order": 2,
      "punctuation_ngram_order": 2,
      "affix_length": 3,
      "min_affix_word_length": 5,
      "max_reported": 25,
      "max_chars_for_ngrams": 500000,
      "section_window_words": 3000,
      "section_shift_threshold": 2.5,
      "k_neighbors": 5,
      "distance_metrics": [
        "cosine",
        "euclidean",
        "manhattan",
        "jensen_shannon"
      ],
      "primary_distance": "cosine",
      "compression_algorithm": "lzma",
      "min_corpus_documents": 4,
      "min_documents_per_author": 2,
      "outlier_threshold": 3.5,
      "seed": 42,
      "word_frequency_vocab_cap": 3000,
      "embedding_model": "all-MiniLM-L6-v2",
      "embedding_primary_distance": "cosine",
      "impostors_k": 10,
      "impostors_iterations": 25,
      "impostors_feature_fraction": 0.5,
      "impostors_min_authors": 2,
      "impostors_target_author": null,
      "impostors_representation": "function_words",
      "impostors_classifier_iterations": 15,
      "impostors_classifier_type": "logistic_regression",
      "ncd_corpus_dirs": [],
      "ncd_max_reference_documents": 10,
      "ncd_max_bytes": 100000,
      "pystylometry_max_reference_documents": 5,
      "pystylometry_max_bytes": 200000,
      "pystylometry_mfw": 200
    },
    "anomaly_suite": {
      "enabled": false,
      "_features_requires": {
        "sklearn_detectors": "'isolation_forest', 'lof', 'one_class_svm', 'elliptic_envelope', 'mahalanobis', 'knn', 'pca' and 'gmm' all need scikit-learn (pip install scikit-learn). Without it every one of those findings reports 'unavailable' naming scikit-learn; the pyod- and hdbscan-backed detectors are unaffected.",
        "pyod_detectors": "'hbos', 'ecod', 'copod', 'abod', 'kde' and 'sos' need PyOD (pip install pyod). Without it those six findings report 'unavailable' naming PyOD; every scikit-learn/hdbscan detector still runs. 'sos' can also report 'unavailable' on its own if the reference corpus is too small/degenerate for its affinity computation to stay finite, independent of whether PyOD is installed.",
        "hdbscan": "needs the hdbscan package (pip install hdbscan). Without it 'hdbscan' reports 'unavailable' naming the package; it also reports 'unavailable' (naming this instead) when the reference corpus's feature space has no real density-cluster structure at hdbscan_min_cluster_size, which is a legitimate outcome, not a bug -- see the module docstring."
      },
      "features": {
        "isolation_forest": true,
        "lof": true,
        "one_class_svm": true,
        "elliptic_envelope": true,
        "mahalanobis": true,
        "knn": true,
        "pca": true,
        "gmm": true,
        "hbos": true,
        "ecod": true,
        "copod": true,
        "abod": true,
        "kde": true,
        "sos": true,
        "hdbscan": true,
        "consensus_count": true,
        "disagreement": true
      },
      "columns": null,
      "min_coverage": 0.7,
      "max_features": 20,
      "min_corpus_documents": 20,
      "seed": 42,
      "loo_reference_size": 15,
      "consensus_percentile": 90.0,
      "evidence_features": 5,
      "isolation_forest_n_estimators": 50,
      "one_class_svm_nu": 0.1,
      "pca_components": null,
      "gmm_components": 2,
      "hdbscan_min_cluster_size": 5
    },
    "distribution_distance_suite": {
      "enabled": false,
      "sources": [
        "sentence_words",
        "paragraph_words",
        "paragraph_sentences",
        "word_characters",
        "sentence_commas",
        "turn_words"
      ],
      "features": {
        "wasserstein": true,
        "energy": true,
        "ks": true,
        "cramer_vonmises": true,
        "anderson_darling": true,
        "jensen_shannon": true,
        "kl_divergence": true,
        "hellinger": true,
        "bhattacharyya": true,
        "total_variation": true,
        "mmd": true,
        "quantile_vector": true,
        "tail": true,
        "optimal_transport": true,
        "distance_correlation": true
      },
      "histogram_bins": 16,
      "histogram_smoothing": 0.5,
      "quantile_vector_points": [
        0.1,
        0.25,
        0.5,
        0.75,
        0.9
      ],
      "tail_quantile": 0.1,
      "mmd_max_sample": 500,
      "distance_correlation_max_sample": 300,
      "distance_correlation_pairs": {
        "sentence": ["sentence_words", "sentence_commas"],
        "paragraph": ["paragraph_words", "paragraph_sentences"]
      },
      "min_distinct_values": 5,
      "_requires": "scipy (already in requirements.txt) for wasserstein_distance, energy_distance, ks_2samp, cramervonmises_2samp and anderson_ksamp. Without it, every wasserstein/energy/ks/cramer_vonmises/anderson_darling finding reports 'unavailable' naming scipy; the histogram-based (jensen_shannon/kl_divergence/hellinger/bhattacharyya/total_variation), quantile_vector, tail, mmd, optimal_transport and distance_correlation channels are dependency-free (plain math/statistics) and are unaffected.",
      "_cramer_vonmises_requires": "at least min_distinct_values (default 5) distinct values on BOTH the manuscript side and the corpus reference curve; below that, ties alone can produce a false 'highly significant' result (measured: p=1.5e-9 between two samples of the SAME 10-value discrete distribution), so this channel reports 'unavailable' naming the reason instead of a misleading number. anderson_darling shares this same gate.",
      "_distance_correlation_requires": "nothing extra -- hand-implemented, no package. It compares two of THIS document's own paired sequences (distance_correlation_pairs), never the corpus, because the corpus profile stores only marginal quantile curves, not joint per-item samples; see the module docstring's 'Distance correlation' section for why.",
      "_reference_representation": "every wasserstein/energy/ks/cramer_vonmises/anderson_darling/jensen_shannon/kl_divergence/hellinger/bhattacharyya/total_variation/mmd/quantile_vector/tail/optimal_transport finding compares the manuscript's own raw per-item sample against the corpus's pooled 101-point quantile curve (profile['item_distributions'][source]['quantiles']), NOT the corpus's raw pooled values (textgrader.corpus never stores those) and NOT a distribution of per-book summary averages (those already exist separately as style.mattr-style scalar corpus comparisons). Every finding's distribution.reference field says so."
    },
    "mechanical_quality_suite": {
      "enabled": false,
      "features": {
        "typography": true,
        "encoding": true,
        "encoding_ftfy": true,
        "confusables": true,
        "hyphenation": true,
        "spelling_pyspellchecker": true,
        "spelling_symspell": true,
        "spelling_disagreement": true,
        "likely_typos": true,
        "doubled_words": true,
        "fused_tokens": true,
        "ocr_substitution": true,
        "confusion_pairs": true,
        "sentence_summary": true
      },
      "_encoding_ftfy_requires": "needs the 'ftfy' package (pip install ftfy) for the diagnostic (non-mutating) repair-rate finding; without it both punct.mechanical_ftfy_repair_rate and punct.mechanical_mojibake_pattern_rate report 'unavailable', since the mojibake pattern scan is published alongside the ftfy estimate as a deliberate disagreement pair rather than as its own feature flag.",
      "_confusables_requires": "needs the 'confusable_homoglyphs' package (pip install confusable-homoglyphs); without it punct.mechanical_homoglyph_rate reports 'unavailable'. Only word tokens containing a non-ASCII character are ever examined, which is normally zero of them in English prose, so this feature is cheap regardless of document length.",
      "_spelling_pyspellchecker_requires": "needs the 'pyspellchecker' package (pip install pyspellchecker); without it both unknown-word-rate findings for this checker report 'unavailable', and likely_typos/ocr_substitution/hyphenation fall back to whichever of pyspellchecker/symspellpy is available, or report reduced-confidence/unavailable if neither is.",
      "_spelling_symspell_requires": "needs the 'symspellpy' package (pip install symspellpy), whose bundled frequency dictionary is English only -- a non-English `language` degrades this feature (and fused_tokens, which also needs symspellpy) to 'unavailable' naming that reason. Without the package, both unknown-word-rate findings for this checker report 'unavailable'.",
      "_spelling_disagreement_requires": "needs BOTH spelling_pyspellchecker and spelling_symspell's packages installed; missing either one degrades lexical.mechanical_checker_disagreement_rate to 'unavailable' naming which package is missing.",
      "_likely_typos_requires": "needs the 'pyspellchecker' package (its own edit-distance-1 dictionary lookup, not SymSpell's); without it lexical.mechanical_likely_typo_rate_narration, lexical.mechanical_likely_typo_rate_dialogue and lexical.mechanical_recurring_vocabulary_size all report 'unavailable'.",
      "_fused_tokens_requires": "needs the 'symspellpy' package for real word segmentation; without it lexical.mechanical_fused_token_rate reports 'unavailable'. Independent of spelling_pyspellchecker/spelling_symspell being enabled.",
      "_ocr_substitution_requires": "needs at least one of pyspellchecker/symspellpy installed to confirm a corrected reading is a real word; without either, lexical.mechanical_ocr_substitution_rate reports 'unavailable'.",
      "_hyphenation_requires": "works without any package (raw line-wrap-hyphenation counting), but is far more precise with pyspellchecker or symspellpy installed to confirm the reconstructed word is real; without either, the finding's warning says the count is unconfirmed.",
      "language": "en",
      "recurring_min_count": 3,
      "likely_typo_max_count": 2,
      "likely_typo_max_candidates": 500,
      "fused_min_length": 8,
      "fused_max_candidates": 400,
      "max_reported": 25
    },
    "syntax_complexity_suite": {
      "enabled": false,
      "features": {
        "tunit_clause": true,
        "phrasal_elaboration": true,
        "dependency_topology": true,
        "syntactic_surprisal": true,
        "constituency": false
      },
      "_requires_constituency": "features.constituency needs the 'benepar' package (pip install benepar) plus a one-time download of its pretrained checkpoint (python -c \"import benepar; benepar.download('benepar_en3')\"), about 260 MB on disk. Loading it (a spaCy pipeline with benepar attached) measured about 5.6 seconds and 1.2 GB resident; parsing itself measured about 1.5 seconds per sentence on CPU. Without either the package or the model, every syntax.complexity_constituency_* metric id reports 'unavailable' naming which is missing. That is why this feature never parses a whole document: it samples constituency_sample_sentences individual sentences spread across the book with a fixed constituency_seed, capped at constituency_max_sentences total sentences and constituency_max_seconds of wall time (default 180 seconds). If the time cap is reached, every syntax.complexity_constituency_* finding's sample_size (sentences actually parsed) drops below its min_sample (sentences the deterministic sample called for), which grade.py reports as insufficient_data and withholds from corpus comparison automatically. The flag defaults to false and this note is stripped before it ever reaches the metric, so nothing downloads or loads unasked.",
      "_requires_syntactic_surprisal": "features.syntactic_surprisal (on by default) only ever produces a value against a corpus profile built with BOTH syntax_complexity_suite enabled AND --parse-metrics passed to the profile build -- enabling the suite in metrics config is not enough on its own: this suite's cost is 'parse', so textgrader.corpus._metric_names drops it from profiling (and profile_vector never runs) unless --parse-metrics is set, the same two-part gate coherence_suite's _requires_transition_corpus_delta note describes for its own profile_vector hook (that one also needs --model-metrics; this one does not, since nothing here needs sentence_transformers). It is never fit on the document being scored (see the module docstring). Without a profile built with --parse-metrics, syntax.complexity_pos_bigram_cross_entropy, syntax.complexity_dependency_label_bigram_cross_entropy and syntax.complexity_sentence_syntactic_surprisal all report 'unavailable' naming that requirement; every other feature in this suite is unaffected.",
      "long_dependency_threshold": 10,
      "constituency_model": "benepar_en3",
      "constituency_sample_sentences": 30,
      "constituency_max_sentences": 60,
      "constituency_max_seconds": 180.0,
      "constituency_seed": 0
    },
    "parser_consensus": {
      "enabled": false,
      "features": {
        "segmentation": true,
        "tokenization": true,
        "pos": true,
        "dependency": true,
        "chunks": true,
        "spacy_md": false,
        "spacy_lg": false,
        "stanza": false,
        "constituency_benepar": false
      },
      "_requires_segmentation": "needs no download beyond what a cost=\"parse\" metric already pays for: pySBD (already a project dependency), spaCy's shared en_core_web_sm parse, and the pure-Python 'syntok' package (pip install syntok, in requirements.txt); the 'nltk_punkt' segmenter additionally needs the NLTK 'punkt_tab' data downloaded (python -m nltk.downloader punkt_tab), separately from the nltk package itself. Any one of the seven segmenters (builtin, pysbd, pysbd_quote_aware, nltk_punkt, spacy_sentencizer, spacy_parser, syntok) that is unavailable degrades to its own 'reason' in syntax.parser_segmenters_available's distribution rather than failing the suite; comparisons run over however many are left, down to none (which reports insufficient/unavailable consensus rather than pretending a comparison happened).",
      "_requires_tokenization": "same dependencies as segmentation above (spaCy, NLTK's punkt_tab data, syntok); the canonical Unicode word tokenizer is always available (no package).",
      "_requires_pos_and_dependency": "needs a SECOND parser beyond the always-available spaCy en_core_web_sm pipeline: enable features.spacy_md (python -m spacy download en_core_web_md, ~40MB), features.spacy_lg (python -m spacy download en_core_web_lg, ~560MB) or features.stanza (see _requires_stanza below). With only spaCy sm available, every syntax.parser_pos_*/syntax.parser_uas/syntax.parser_las/syntax.parser_root_*/syntax.parser_dep_label_*/syntax.parser_parse_depth_*/syntax.parser_dependency_distance_*/syntax.parser_alignment_coverage finding reports that plainly instead of comparing spaCy sm against itself. Only the first two available parsers, in the fixed priority order spacy_sm, spacy_md, spacy_lg, stanza, are ever compared (never a full pairwise matrix), and every finding's distribution names exactly which two produced it.",
      "_requires_stanza": "needs the 'stanza' package (pip install stanza, in requirements-embeddings.txt since it depends on torch) PLUS its English models downloaded separately -- this suite never downloads them itself (every stanza.Pipeline call here passes download_method=None): run python -c \"import stanza; stanza.download('en', processors='tokenize,mwt,pos,lemma,depparse')\" first (about 320MB on disk, verified in this environment; loading the pipeline measured about 4.6s and parsing one sentence about 0.4-1s on a quiet CPU-only machine). Without either the package or the downloaded models, features.stanza degrades to naming exactly which is missing in syntax.parser_pos_agreement_rate's warning (or whichever finding needed a second parser) rather than crashing.",
      "_requires_chunks": "spaCy's noun_chunks (dependency-derived) is always available once spaCy itself is; the SECOND, independent noun-phrase source syntax.parser_np_chunk_agreement needs is either features.stanza with a 'constituency' processor in stanza_processors (see _requires_stanza above; constituency models are a separate stanza download, python -c \"import stanza; stanza.download('en', processors='tokenize,mwt,pos,constituency')\") or features.constituency_benepar (see below). Without either, this one finding reports 'unavailable' naming that a second source is needed; every other channel in this suite is unaffected.",
      "_requires_constituency_benepar": "needs the 'benepar' package (pip install benepar) plus its own downloaded parser model (python -c \"import benepar; benepar.download('benepar_en3')\"), about 260 MB, and torch from requirements-embeddings.txt. benepar calls a T5 tokenizer method that transformers 5.x removed; TextGrader restores it (textgrader.optional.shim_benepar_transformers), the same fix syntax_complexity_suite uses. Without the package or the model, syntax.parser_np_chunk_agreement reports 'unavailable' naming which is missing. Off by default independent of every other feature in this suite.",
      "segmenters": ["builtin", "pysbd", "pysbd_quote_aware", "nltk_punkt", "spacy_sentencizer",
                    "spacy_parser", "syntok"],
      "long_sentence_words": 40,
      "max_paragraphs": 200,
      "max_words": 20000,
      "max_sentences_for_parse": 300,
      "max_seconds_parse": 180.0,
      "max_reported": 10,
      "seed": 0,
      "spacy_md_model": "en_core_web_md",
      "spacy_lg_model": "en_core_web_lg",
      "stanza_processors": "tokenize,mwt,pos,lemma,depparse",
      "benepar_model": "benepar_en3"
    },
    "lexical_norms_suite": {
      "enabled": false,
      "features": {
        "concreteness": true,
        "age_of_acquisition": true,
        "warriner_vad": true,
        "nrc_vad": true,
        "sensorimotor": true,
        "subtlex": true,
        "glasgow": true,
        "mrc": true,
        "frequency_source_agreement": true,
        "lexdiv_crosscheck": true,
        "taaled_crosscheck": true,
        "lemma_lookup": false,
        "lftk_crosscheck": false
      },
      "_requires_concreteness": "needs the Brysbaert/Warriner/Kuperman concreteness norms cached: run `python -m textgrader.lexicons download brysbaert_concreteness` (an .xlsx file, ~2MB; also needs the 'openpyxl' package, pip install openpyxl) or set resource_paths.brysbaert_concreteness to a local copy. Without either, every lexical.norm_concreteness_* finding reports 'unavailable' naming that reason; every other feature is unaffected.",
      "_requires_age_of_acquisition": "needs the Kuperman age-of-acquisition norms cached: `python -m textgrader.lexicons download kuperman_aoa` (.xlsx, ~4MB; needs openpyxl, see above). Without it, lexical.norm_aoa_*/lexical.norm_aoa_lemma_* report 'unavailable'.",
      "_requires_warriner_vad": "needs the Warriner/Kuperman/Brysbaert VAD norms cached: `python -m textgrader.lexicons download warriner_vad` (.csv, ~4MB; no extra package). Without it, lexical.norm_warriner_valence_*/_arousal_*/_dominance_* report 'unavailable'.",
      "_requires_nrc_vad": "needs the NRC VAD Lexicon v2.1 cached: `python -m textgrader.lexicons download nrc_vad` (a ~6MB zip, fetched from saifmohammad.com and unpacked to its unigram file; no extra package). Free for non-commercial research use per the resource's own README. Without it, lexical.norm_nrc_valence_*/_arousal_*/_dominance_* report 'unavailable'; this is kept as a fully independent resource from warriner_vad even though both measure valence/arousal/dominance (rule 6 of the task's cross-task checklist).",
      "_requires_sensorimotor": "needs the Lancaster Sensorimotor Norms cached: `python -m textgrader.lexicons download lancaster_sensorimotor` (.csv, ~17MB; no extra package). Without it, lexical.norm_sensorimotor_perceptual_*/_action_* report 'unavailable'.",
      "_requires_subtlex": "needs SUBTLEX-US cached: `python -m textgrader.lexicons download subtlex_us` (.tsv, ~4MB, fetched from the openlexicon mirror since crr.ugent.be -- the paper's own host -- is currently down; no extra package). Without it, lexical.norm_subtlex_zipf_*/_contextual_diversity_* and lexical.norm_frequency_source_agreement report 'unavailable'.",
      "_requires_glasgow": "needs the Glasgow Norms cached: `python -m textgrader.lexicons download glasgow_norms` (.xlsx, ~1MB; needs openpyxl, see concreteness above). Without it, all nine lexical.norm_glasgow_* findings report 'unavailable'.",
      "_requires_mrc": "needs the MRC Psycholinguistic Database's machine-usable dictionary cached: `python -m textgrader.lexicons download mrc` (a ~11MB fixed-width text file, mirrored from a community port of the original mrc2.dct; no extra package). Only about 3-9 thousand of its 150,837 headwords carry human ratings for any given dimension -- 0 in the source means 'not rated' and this module treats it as missing, not as a score of zero -- so mrc's coverage_pct will legitimately be much lower than the other resources here even on a common-vocabulary text. Without the cache, all six lexical.norm_mrc_* findings report 'unavailable'.",
      "_requires_frequency_source_agreement": "needs both 'wordfreq' (already a project dependency) and the subtlex resource cached (see _requires_subtlex above). Without either, lexical.norm_frequency_source_agreement reports 'unavailable' naming which is missing.",
      "_requires_lexdiv_crosscheck": "needs the 'lexical_diversity' package (pip install lexical-diversity, in requirements.txt; confirmed to be Kristopher Kyle's own lightweight lexical-diversity implementation, not a same-named unrelated package -- see lexical_norms_suite's module docstring). Computed over at most diversity_crosscheck_max_tokens tokens (default 50,000) since the package is not written for book-length input; the six lexical.norm_lexdiv_* findings note when their input was truncated. Without the package, all six report 'unavailable'.",
      "_requires_taaled_crosscheck": "needs the 'taaled' package (pip install taaled, in requirements.txt; confirmed by its own docstring to be TAALED 'second generation' itself, by the same author as lexical_diversity above -- see the module docstring). Also pulls in 'pylats' as taaled's own dependency and prints a harmless 'plotnine has not been installed' notice on import that this suite never triggers (no plotting is requested). Same diversity_crosscheck_max_tokens/_window truncation as lexdiv_crosscheck above. Deliberately does NOT call TAALED's own MATTR (its sliding-window implementation measured O(tokens*window), impractically slow at book length; lexical_diversity's MATTR above already cross-checks that statistic) -- only MTLD/HD-D/MSTTR/RTTR/Maas are reported. Without the package, those five lexical.norm_taaled_* findings report 'unavailable'.",
      "_requires_lemma_lookup": "off by default: forces the shared spaCy parse (tens of seconds on a novel), which this suite's declared 'moderate' cost class does not otherwise pay for. Needs 'spacy' plus its en_core_web_sm model AND kuperman_aoa cached (see _requires_age_of_acquisition above). Without either, lexical.norm_aoa_true_lemma_mean reports 'unavailable' naming which is missing.",
      "_requires_lftk_crosscheck": "off by default, same parse-cost reason as lemma_lookup above. Needs the 'lftk' package (pip install lftk, in requirements.txt; confirmed by import -- lftk.Extractor takes a spaCy Doc and lftk.search_features lists genuine linguistic feature keys) plus spaCy's en_core_web_sm model. Computed over at most lftk_max_chars characters (default 200,000). LFTK bundles its OWN copies of Kuperman AoA, a Brysbaert-sourced AoA and SUBTLEX-US Zipf -- these three findings are that independent bundled table, kept beside (never replacing) this suite's own downloaded kuperman_aoa/subtlex_us numbers, so the two disagreeing is itself data. Without the package or the model, all three lexical.norm_lftk_* findings report 'unavailable'.",
      "language": "en",
      "resource_paths": {},
      "diversity_crosscheck_max_tokens": 50000,
      "diversity_crosscheck_window": 50,
      "lftk_max_chars": 200000
    }
  },
  "allow_external_metric_commands": false,
  "project_rules": {
    "banned_phrases": [],
    "em_dash": null,
    "quote_style": null,
    "hard_line_breaks": null,
    "chapter_length": null
  },
  "project_measures": {}
}
